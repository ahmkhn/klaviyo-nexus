# Klaviyo OAuth App credentials (from Manage apps)
KLAVIYO_CLIENT_ID=
KLAVIYO_CLIENT_SECRET=

# Must match exactly one of the Redirect URLs configured in Klaviyo app settings
# Use localhost for local dev, or an ngrok/hosted URL when needed.
KLAVIYO_REDIRECT_URI=http://localhost:8000/auth/callback

# Frontend URL your callback redirects to
FRONTEND_URL=http://localhost:3000

# (Optional) If you’re using OpenAI
OPENAI_API_KEY=